import styles from "./styles.module.css";

export const LoginUser = () => {
  return (
    <section
      role="region"
      aria-label="ログインユーザー"
      className={styles.user}
    ></section>
  );
};
