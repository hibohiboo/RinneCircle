import { Meta, StoryObj } from "@storybook/react";
import { ContentFooter } from "./";

export default {
  component: ContentFooter,
} as Meta<typeof ContentFooter>;

type Story = StoryObj<typeof ContentFooter>;

export const Default: Story = {};
